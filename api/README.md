# redirect to api folder to run the api
cd .\api\  

# create env if you running for the first time
python -m venv venv

# to activate the env
venv\Scripts\activate

# to run the app
python app.py

# install all the libraries if you running for the first time
pip install -r requirements.txt
